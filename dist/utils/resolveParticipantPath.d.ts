import type { Paths } from '../types/paths';
import { ExtendedDailyParticipant } from '../DailyParticipants';
export declare const resolveParticipantPath: (participant: ExtendedDailyParticipant, path: Paths<ExtendedDailyParticipant>) => any;
